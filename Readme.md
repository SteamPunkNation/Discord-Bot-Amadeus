# Amadeus Discord Bot

In development.
To start bot type

> npm run start

To test for errors type

> npm run lint

You can edit lint in the package.json to add eslint . --fix to automatically fix errors in the code.

## Setup

In order to starup your own version you must rename the temp-config.json file in the root directory to config.json and replace the values with your own.
